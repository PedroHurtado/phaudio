Tareas
	1. Crear diferentes packages(x)
		Dependendias externas no se deben de copiar al buddle(x)
	2. Conectar con la api de 160world(x)
	3. Refactor->control de exceptions(x)
	4. Enviar al servidor los ultimos bytes de voz y 
	capturar errores en el hilo principal

	pnpm changeset
	pnpm changeset version
	pnpm changeset publish